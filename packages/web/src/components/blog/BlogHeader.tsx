/** BlogHeader — thin sticky bar shown above the hero. No logo here; logo appears in HeroSection. */
export default function BlogHeader() {
    return (
        <header className="sticky top-0 z-50 bg-white/80 dark:bg-gray-950/80 backdrop-blur-md border-b border-gray-200 dark:border-gray-800 transition-colors duration-300 h-14" />
    )
}
